# Results placeholder

Put generated files here, for example:

- baseline grid CSV
- markdown validation notes
- small tables exported from experiments
- plots produced later

Keeping this folder in the scaffold makes it obvious where reproducible outputs should go.
